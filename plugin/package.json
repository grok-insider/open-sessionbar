{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "opencode-sessionbar",
  "version": "0.1.0",
  "description": "Serves live opencode session state (titles, status, waiting-for-permission) over localhost HTTP + SSE so a waybar module can render a SessionBar.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "exports": {
    "./tui": {
      "import": "./tui.tsx"
    }
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "*",
    "@opentui/core": "*",
    "@opentui/solid": "*",
    "solid-js": "*"
  },
  "engines": {
    "opencode": ">=1.3.14"
  }
}
